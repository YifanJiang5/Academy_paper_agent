# 优化事项

## 1.文档解析模块

需要优化目前pdf文件所用的加载器**PdfLoader**，改用MinerU的精准解析api来解析pdf文件。

### 1.1 MinerU使用

MinerU api文档：https://mineru.net/apiManage/docs

MinerU api key：通过 `MINERU_API_KEY` 环境变量配置，不在代码或文档中保存明文密钥。

### 1.2 MinerU解析结果

MinerU解析后，会将文件保存为full.md文件，原文中的图、表等会被截取成图片保存在images文件夹，原文中相应位置的图会被替换为图片地址的占位，原文中的表被转化为html格式，同时相应位置也会有一个图片地址的占位。

之后的切片与增强操作，基于MinerU的解析结果。



当前仅需要修改项目中的文档解析模块，修改后需要适配现有项目中的其余接口。

### 1.3 行动规划

#### 1.3.1 解析流程

1. 保持当前 `PdfLoader.load(file_path) -> Document` 接口不变，避免后续 Pipeline、Splitter、Transform、Embedding、Storage 大范围改动。
2. 在 `PdfLoader` 内新增 MinerU 解析流程：申请批量上传链接、上传 PDF、轮询批量解析结果、下载并解压 `full_zip_url` 结果包。
3. 从 MinerU 结果包中定位 `full.md`，将其作为 `Document.text` 的基础内容；表格 HTML 保留在正文中，继续参与后续切片和增强。
4. 解析 `full.md` 中的 Markdown 图片与 HTML `<img>` 标签，将其转换为项目现有的 `[IMAGE: image_id]` 占位符。
5. 将 MinerU 输出的图片复制到当前项目图片目录，并生成兼容现有 `metadata.images` 的图片元数据，使 `DocumentChunker` 和 `ImageCaptioner` 能继续按已有逻辑工作。

#### 1.3.2 兼容与兜底

1. MinerU API Key 从 `MINERU_API_KEY` 环境变量读取，也支持初始化 `PdfLoader` 时显式传入。
2. MinerU 未配置、请求失败、解析失败、结果包缺失或结果为空时，允许回退到当前 MarkItDown 解析流程。
3. 保留 `extract_images` 参数：关闭时不额外生成图片元数据；开启时优先使用 MinerU 产物中的图片。
4. 在 `Document.metadata` 中记录实际解析器来源，例如 `parser=mineru` 或 `parser=markitdown`，便于追踪和排查。

#### 1.3.3 验证计划

1. 增加 MinerU 产物适配的单元测试：验证 `full.md` 图片占位替换、图片复制、`metadata.images` 结构兼容。
2. 增加 MinerU 失败回退测试：模拟 MinerU 解析异常，确认 `PdfLoader` 自动回退到 MarkItDown。
3. 运行 Loader 相关单元测试与 PDF Loader 集成测试，确认原有接口和下游依赖不破坏。

## 2.文档切片模块

### 2.1 切分单位

目前是按照字符进行切分的，需要修改为**根据单词数量**进行切分

如"hello word"应该计数为2个单词

### 2.2 切分符号

在mineru解析后的full.md文件中，会将论文的小标题标上"#"

如：**# Abstract**/**# 1Introduction**等

在切分中，首先**按照小标题进行切分**，再在**每个部分内部（比如introduction内部）进行递归切分**

目前RecursiveCharacterTextSplitter 使用的是项目里自定义的 DEFAULT_SEPARATORS，按这个顺序递归尝试切分：

```
[
    "\n\n",  # 段落
    "\n",    # 换行
    ". ",    # 英文句号后空格
    "! ",
    "? ",
    "; ",
    ", ",
    " ",     # 空格
    "",      # 最后退化到逐字符切分
]
```

根据学术论文的文章结构，需要将分隔符修改为**只用"\n\n"作为分割符号**。

### 2.3 关于切分token上限

在每个小标题内部进行切分时，目前设置的chunk_size=1000，对于“只用 \n\n 切分”的策略，chunk_size 就不应该再被理解成硬上限，而应该是**段落组合时的目标上限**。

也就是说，合理行为应该是：

- 多个段落可以合并成一个 chunk，直到接近 chunk_size；
- 一旦再加入下一个段落会超过 chunk_size，就在段落边界切开；
- 如果**单个段落本身就超过 chunk_size**，也不在段内强行切分，而是保留为一个超长 chunk。

目前dashboard中显示的每个chunk字符的数量，需要修改为**显示单词的数量**

## 3.数据增强模块

### 3.1 refinement

目前代码中，会利用llm/rules对chunk进行refine，由于现在文档解析的质量较高，可以去除此步骤。

### 3.2 enrichment

enrichment的作用是为原始chunk增加metadata，目前的metadata结构如下：

```
{
"page_num":2
"images":"{'id': '60dad628_2_1', 'path': 'data/images/default/60dad6285db071906aa76534f4cd937bf4e56f7f8083b64057d5408d582ef25c/60dad628_2_1.jpg', 'page': 2, 'text_offset': 5154, 'text_length': 21, 'position': {'width': 497, 'height': 406, 'page': 2, 'index': 1, 'source': 'mineru', 'original_src': 'images/ba48412ab51b0841e17f63208a250887116526e41d9f93f93220447e51463b7b.jpg', 'bbox': [524, 210, 816, 395], 'type': 'image'}}"
"tags":"2,It,Large Language Models,Originally,They,Unfortunately"
"image_refs":"60dad628_2_1"
"parser":"mineru"
"enriched_by":"rule"
"text":"# 1Introduction\n\nLarge Language Models (LLMs) have demonstrated impressive capabilities across a wide variety of natural language processing tasks [70,71, 54,68,8]. （部分内容省略）"
"mineru_output_dir":"/Users/yifan/llm/MODULAR-RAG-MCP-SERVER/data/mineru/default/60dad6285db071906aa76534f4cd937bf4e56f7f8083b64057d5408d582ef25c"
"refined_by":"rule"
"mineru_full_md_path":"/Users/yifan/llm/MODULAR-RAG-MCP-SERVER/data/mineru/default/60dad6285db071906aa76534f4cd937bf4e56f7f8083b64057d5408d582ef25c/full.md"
"title":"1Introduction"
"doc_hash":"60dad6285db071906aa76534f4cd937bf4e56f7f8083b64057d5408d582ef25c"
"source_path":"/private/var/folders/g0/1w62smnx1bg1r7_zxbc7xhpr0000gp/T/tmpihymgebd.pdf"
"doc_type":"pdf"
"summary":"# 1Introduction\n\nLarge Language Models (LLMs) have demonstrated impressive capabilities across a wide variety of natural language processing tasks [70,71, 54,68,8]. They are increasingly deployed in realworld setings,including in high-stakes domains such as medicine, journalism,or legal services [65,76, 53, 63]. It is therefore paramount that we can trust the outputs of LLMs."
"chunk_id":"9fdf98e5_0002_72b575a3"
"word_count":973
"enrich_fallback_reason":"llm_failed"
"chunk_index":2
"source_ref":"doc_60dad6285db07190"
}
```

说明和需要修改的点：

1. "summary"和"tags"字段从metadata中去除；
2. "title"字段储存当前论文的总标题，以文档解析后的第一个子标题的内容作为文章总标题；
3. 新增"sub-title"字段，储存当前chunk所属的子标题，如"Abstract"；
4. enrichment阶段不再需要运用规则或大模型，因此相应字段"enriched_by"，"refined_by"，"enrich_fallback_reason"也不再需要。

### 3.3 图/表增强

#### 3.3.1 图的处理

如果chunk中含有图片，相应位置会用一个占位符表示（如[IMAGE: 60dad628_2_1]），同时metadata中的"images"字段会储存图片的具体信息。

解析后的文档中，图片占位符后会紧跟着其图注。在让llm生成图片描述时，输入为**prompt+图注+原始图片**；

对于prompt，先检查目前代码中在使用llm进行图片描述时是否用了prompt：如果使用了，请向我报告prompt的形式，如果没有使用，则加上prompt，大概形式为：“这是学术论文中的一幅图像，请根据以下图片及其表注，生成对其的描述，用于后续的检索增强操作。要求简洁精炼，但要涵盖图中的信息”。具体的prompt请你进一步优化。

llm对图像生成描述后：1. 用生成的内容替换chunk原文中的图像占位符；2. 将生成的内容拼接到metadata的"text"字段中对应图像占位符的后面（不是替换而是拼接）。

#### 3.3.2 表的处理

文档解析后，表格以html的格式储存，并会被<table>...</table>块包裹。如果chunk中含有<table>...</table>块，则需要对表进行增强处理。

在html表格的上方（隔一行）会有该表的表注。让llm生成图片描述时，输入为**prompt+表注+html格式表格**；对于prompt，目前代码中应该没有对表格进行数据增强的逻辑，因此prompt大概形式为：“这是学术论文中的一幅表格，请根据以下html格式表格及其表注，生成对其的描述，用于后续的检索增强操作。要求简洁精炼，但要涵盖表中的信息”，具体的prompt请你进一步优化。

llm生成对表格的描述后：1. 用生成的内容替换chunk原文中的html格式的表格；2. metadata中"text"字段中的相关内容不需要变动。

#### 3.3.3 注意事项

用来进行图标描述的模型选取的是kimi-k2.5，目前代码中模型的解码温度为0，但是该模型的解码温度只能是1，因此这里需要改动。

## 优化完成情况

| 优化项 | 当前状态 | 完成时间 | 说明 |
| --- | --- | --- | --- |
| 1. 文档解析模块：PdfLoader 改用 MinerU 精准解析 API | 已完成 | 2026-05-13 | 已实现 MinerU 主解析流程、MarkItDown 失败兜底、MinerU 图片占位适配、Pipeline 解析器来源追踪，并补充相关单元测试；已在 `.venv` 中通过 Loader 单元/集成回归测试。 |
| 2. 文档切片模块：按单词数量切片且仅使用段落分隔符 | 已完成 | 2026-05-13 | 已将切片逻辑改为按单词数量统计，先按 Markdown 小标题分区、再在分区内仅按 `\n\n` 段落边界组合 chunk；`chunk_size` 作为段落组合目标上限，单段超限时保留整段，同时已同步修改 Dashboard 的 chunk 大小展示并通过相关单元/E2E 测试。 |
| 3. 数据增强模块：移除文本 refinement、简化 metadata enrichment，并增强图/表描述 | 已完成 | 2026-05-14 | 已从主摄取流程中移除 chunk refinement；metadata enrichment 改为仅保留结构化元数据并清理 `summary`、`tags`、`enriched_by`、`refined_by`、`enrich_fallback_reason` 等旧字段；`title` 统一为论文总标题并新增 `sub-title` 记录 chunk 所属小标题；图片增强改为 prompt+图注+原图，生成描述后替换 chunk 原文占位符并在 metadata `text` 中追加；新增表格增强逻辑，使用 prompt+表注+HTML 表格生成描述并替换 chunk 原文表格；已将 kimi-k2.5 图/表增强调用温度约束为 1，并补充相关单元测试。 |
